import React from "react";
type RowTable = {
  className?: string;
  email: string;
  position: string;
  image?: string;
  tel?: string;
};
export default function RowTable({
  className,
  email,
  image,
  tel,
  position,
}: RowTable) {
  return <></>;
}
